import 'package:flutter/material.dart';

import '../shared.dart';

class EventsTools {
  static Map<String, Color> eventsTypes = {
    "Paiement": AppColors.greenO,
    "Jours restants": AppColors.yellow,
    "Convocation": AppColors.orange,
    "Fin du contrat": AppColors.red
  };
}
